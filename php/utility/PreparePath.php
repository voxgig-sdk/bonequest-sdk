<?php
declare(strict_types=1);

// Bonequest SDK utility: prepare_path

class BonequestPreparePath
{
    public static function call(BonequestContext $ctx): string
    {
        $point = $ctx->point;
        $parts = [];
        if ($point) {
            $p = \Voxgig\Struct\Struct::getprop($point, 'parts');
            if (is_array($p)) {
                $parts = $p;
            }
        }
        return \Voxgig\Struct\Struct::join($parts, '/', true);
    }
}
